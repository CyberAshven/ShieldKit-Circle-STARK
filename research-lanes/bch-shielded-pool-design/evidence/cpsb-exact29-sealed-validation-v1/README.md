# CPSB exact29 sealed-validation v1

Retention-only authenticated sealed static validation evidence captured from
`/tmp/cpsb-exact29-v1-validate-sealed.K3yrTq/`.

Authorization: **NONE**. This bundle grants no authority and makes no admission,
attempt, authority, measurement, FRI qualification, FRI ranking, FRI selection,
prover, or PoolAction refreeze claim. It does not authorize any such activity.

The receipt is unsigned and replayable. Its evidence boundary includes the
external caller and host trusted-computing base; this bundle does not convert
those dependencies into in-package authority or independent attestation.

The retained source artifacts are pinned exactly:

- `receipt.json`: 8,147 bytes; SHA-256
  `1fbe919d8556f6e73344402d07b3a1df8d47fca0d91268fadbb4d8c7d9f58f53`.
- `stderr.bin`: 0 bytes; SHA-256
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.

Both artifacts are preserved as opaque byte-for-byte copies; `receipt.json`
was not reserialized and `stderr.bin` is the retained zero-byte stderr
artifact. Integrity is recorded in `SHA256SUMS`.

This exact29 sealed-validation retention bundle is distinct from, and does not
supersede or reinterpret, the exact27 source-validation retention bundle.
