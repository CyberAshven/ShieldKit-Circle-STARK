# CPSB exact27 source-validation v1

Retention-only evidence bundle for the raw outputs retained from
`/tmp/cpsb-exact27-v1-validate-unsealed.JS7lnI/`.

Authorization: **NONE**. This bundle is not validator, seal, anchor, admission,
attempt, measurement, or FRI evidence. It grants no authority and asserts no
successful validation outcome.

`receipt.json` is preserved as an opaque byte-for-byte copy; it was not parsed
or reserialized. `stderr.bin` is the retained zero-byte stderr artifact.

Integrity is recorded in `SHA256SUMS`.
