# Gate B1 opaque branch-slot language v1

This detached, intentionally unsealed four-file package defines only a closed abstract schema language for an opaque future branch-slot record kind. The external root activation authorizes only this exact target roster and zero-instance schema-language scope; this root grants no slot-instance or post-target authority.

The root remains unratified and uninstantiated. The branch-slot catalog is empty and schema-capped at zero items. All thirty-six counts are zero and all forty authorizations are false. The identifiers `branch-slot:0` and `branch-slot:1` are only package-local, opaque, nonexhaustive work-queue capacity vocabulary. They name no branch, encode no ordering, priority, or semantic meaning, and instantiate nothing.

The helper schema may validate hypothetical objects independently for grammar review. Such validation creates no object and grants no branch-slot, trigger, affected-slot, evidence, feasibility, ruling, relation-semantics, version-selection, refreeze, execution, measurement, lane, credit, or successor-artifact authority.

The accepted trigger is bound only as package-level context by its exact bytes and remains `PROPOSED_NO_CREDIT` with an empty affected-slot list. This package cannot mutate or progress it. The relation reference remains null, relation semantics remain unassigned/null, and no branch or relation version is selected.

The accepted five-file authority package and separate root activation are bound directly. Its transitive sources are not imported as target authority. The lane remains context only, and the source collision remains unresolved.

The root and helper use only the local Draft 2020-12 schema graph under `schemas/`. There is deliberately no manifest, checksum list, validator, test, review anchor, executable, content or semantic digest, package identity, seal, lane binding, populated catalog, helper instance, or additional file.
