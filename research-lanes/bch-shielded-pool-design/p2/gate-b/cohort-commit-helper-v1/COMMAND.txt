Build/check (the object token names a temporary file outside this package):

node build.mjs --write
node build.mjs --check
node --test test.mjs
cd /home/toorik/Projects/ShieldKit-LABS
sha256sum -c research-lanes/bch-shielded-pool-design/p2/gate-b/cohort-commit-helper-v1/SHA256SUMS

Pinned construction commands:

/usr/bin/as --64 -o <temporary-object> renameat2-helper.S
/usr/bin/ld -m elf_x86_64 --build-id=none -z noexecstack -e _start -o renameat2-helper <temporary-object>

Direct interface (caller supplies inherited directory FDs 3 and 4):

renameat2-helper <old-basename> <new-basename>

Inherited fds: 3=old parent directory, 4=new parent directory,
5=retained validated source directory.
