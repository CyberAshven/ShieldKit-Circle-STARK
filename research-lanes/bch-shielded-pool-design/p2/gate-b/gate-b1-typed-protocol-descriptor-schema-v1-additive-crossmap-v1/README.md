# Gate B1 additive crossmap v1

Detached, unsealed administrative crossmap only. It copies source identities, pins, pending markers, unsupported axes, unresolved refreeze-axis names, and RF obligation identifiers without allocating a descriptor language or schema version.

Status: `DRAFT_STATIC_CROSSMAP_UNSEALED_DETACHED_NO_CREDIT`.

The package is not a protocol descriptor, descriptor language, schema-version allocation, source closure, review instance, lane projection, gate reopen, refreeze authority, selection authority, or execution authority. It has no manifest, package identity, lane binding, lane credit, or gate effect. `B1` is closed; `DESCRIPTOR_LANGUAGE_PENDING_ADDITIVE_SCHEMA_VERSION` remains terminal; the PoolAction disposition is `BLOCKED_VERSION_PIVOT_NO_REFREEZE`.

`static-crossmap-v1.json` is constrained by `static-crossmap-v1.schema.json`. No executable artifact is included. `MANIFEST.json` and `SHA256SUMS` are intentionally absent.
