# Gate B1 relation-version pivot decision language v1

This detached, intentionally unsealed six-file package defines only a closed abstract schema language for three future record kinds: pivot triggers, branch feasibility records, and branch rulings. The external root activation authorizes only this exact target roster and schema-language scope; this root grants no downstream target or post-target authority.

The root remains unratified and uninstantiated. All four catalogs are empty, all twenty-four counts are zero, all twenty-seven authorizations are false, the version pivot remains blocked with no refreeze, and every relation, relation-semantics, version, and branch selection binding remains null or unassigned.

The helper schemas may validate hypothetical objects independently for grammar review. Such validation creates no object and grants no trigger, feasibility, ruling, branch, relation, evidence, admission, execution, measurement, selection, credit, lane, refreeze, or successor-artifact authority.

The accepted authority package is bound directly by its exact five raw files and semantic digests. Its transitive sources are not imported as target authority. The lane remains context only, and the source collision remains unresolved.

The root and three helpers use only the local Draft 2020-12 schema graph under schemas/. There is deliberately no manifest, checksum list, validator, test, review anchor, executable, target-root or package content/semantic digest, package identity, seal, or lane binding.
