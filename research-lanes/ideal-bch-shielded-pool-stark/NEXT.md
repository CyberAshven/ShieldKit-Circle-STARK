# Next (this ruleset)

A wall is a number, then the next construction. Parent freeze: **91598 B** on Chipnet, query-conjecture 128, **M31 field ~31**, preimage in unlocking. That is evidence. It is not B under RULES.md.

## First construction that can change §2

See [`FIELD.md`](FIELD.md) (research only, no vk). Candidate: **Stwo QM31 as SecureField**, circle stays M31, hash stays SHA-256. While the lock still draws λ from M31, **do not claim 100 bits**. Query worksheet alone is not 100.

Do not start by packing more Merkle, shrinking A, or dropping queries/grind/TRACE to fit.

## Then, without dropping §2

- RULES §6: leaf↔nf↔amount in the on-chain STARK; unlocking MUST NOT push amount/rho/owner. Anonymity set 1 is a fail.
- RULES §7: generic step program (or equivalent); genesis does not paint walk-in nullifiers; N walks on chain in one standard tx.
- RULES §1 stays gating: tx ≤ 100000, unlocking/redeem ≤ 10000, `createVirtualMachineBch2026(true)`, Electrum.

Walk-in Chipnet `e63ccf03…` (batch-exit-walkin) is a §7 graph prior. It does not satisfy §2 or §6. Do not relabel it as this named end.

## Not a win

First new byte-count wall. Green tests on a weaker statement. Relabeling A, C, or the 91 KB freeze as this B. `standard=false`. “The lane cannot.”
