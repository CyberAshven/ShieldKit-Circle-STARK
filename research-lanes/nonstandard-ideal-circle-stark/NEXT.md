# Next (this ruleset)

A wall is a number, then the next construction. **Start is occupancy FRI10 / 124-bit** ([`START.md`](START.md)). The search is [`PROMPT.md`](PROMPT.md).

## After dedicated booleanity kernels (measured, not the named end)

Live: occupancy tLde = algebraicC Q + booleanity Q + mask. Packed nTable is `(q−R)·Z` (booleanity C). Isolated leftover EQUALVERIFYs that. Envelope **A** is 36-query fused folds in one standard tx (Chipnet `18bc53d6…` / 97736 B, leftover-bind). Envelope **B** is the occupancy completeness successor (Chipnet `6b75a37d…` / **147097 B**, miner-run fused leftover), not FRI9 `81bb2cef…` / 498398 B. Envelope **C** chunks 36 queries across 19 hops (pay `8b7bdbad…` / 92326 B). Occupancy B 3-note silent batch: `a1c406aa…` / 143204 B. CLI: `cd research-lanes/nonstandard-ideal-circle-stark` then `npx tsx src/cli.ts pool measure-tx --envelope a|b|c` / `pool land --envelope a|b|c`. Workflow: [`WORKFLOW.md`](WORKFLOW.md). FRI_VERSION 11.

Fused fold leftover EQUALVERIFYs `(q−R)·Z` against peeled nTable (nFold×4 walked with qTable). SHA-in-C is the three booleanity kernels. Occupancy-only leftover + honest T still VM-rejects. Round-gates stay JS. Leaf/nf compressions stay note-auth.

Do not default to leftover SHA-LDE. Do not default to 36 extra 576-column hash-AIR inputs. Occupancy FRI10 / 124-bit stays. Do not pad.

## Then, without dropping §2 or §6

- RULES §7 walk-in later.
- RULES §1 stays gating.

## Not a win

First new byte-count wall. Green tests on a weaker statement. Relabeling occupancy as shielded. “The lane cannot.”
