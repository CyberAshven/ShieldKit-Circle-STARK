# Goal prompt (paste into `/goal`)

Work only in `research-lanes/nonstandard-ideal-circle-stark`. Do not edit the sibling 100 KB lane, `envelope-b-standard`, or `workspaces/any-amount`. Constitution: `RULES.md`. Start: occupancy FRI10 / QM31 124-bit (`START.md`, Chipnet `60d186de…` / 99043 B). Only the human declares the named end.

**Named end.** One Chipnet-mined May-2026 consensus transaction that is B under RULES.md: tx ≤ 1_000_000 B, unlocking/redeem ≤ 10_000 B, `createVirtualMachineBch2026(false)`, lab BCHN includes it. Circle FRI, `OP_SHA256`, min(FRI-query, field, SZ, hash-RO) ≥ 100. Miner runs every numbered check. Unlocking is silent — no rho, owner, or amount. Leaf↔nf↔amount lives in the lock. Encoding ≡ spec. Walk-in is later, not a substitute for silence.

**The question (this is the work).** Occupancy already has a coherent 18-input verifier: Merkle, bind-T, grind, algebraicC, fused fold+R, leftover as pair-bind. The leak is the last kernel that still needs the preimage. Stop that leak. The interesting object is not another AIR bolted on: it is the most elegant, BCH-native organisation of this same verifier so the money relation is miner-run and the unlocking is silent — without a second unmanageable machine next to the first, and without a transaction that is large because the argument was not.

This is the scientific frontier, and it is discovery. Beauty is inner, in places no one will screenshot. BCH-native means the VM is the paper: each input a numbered check, leftover pair-bind not cargo, density `800×(41+unlocking)` not pad, `OP_SHA256` the hash, introspection the bind, no Poseidon-in-the-lock. A stranger reading the redeem should see the argument. Consensus 1 MB is room to think, not a license to sprawl. Unmanageable means unauditable, ballast, or size that is not the proof. JS-fail / VM-accept is P0.

**Not the aesthetic.** Publishing the note. Dummy pad / packTo / `KERNEL_UNLOCK_PAD` / leftover-fill. Leftover SHA-LDE occupancy walks. Retry-prove to 100 KB. Relabeling occupancy as shielded. Thirty-six extra hash-AIR inputs as the default because they meter ~95 kB. “The lane cannot.” A wall is a number, then the next construction.

Play. Invent the organisation. Measure it. Keep proven, measured, and speculative apart. Do not stop at the first wall, a green suite, or a 10k/100k miss. The object is the silent occupancy verifier, landed — work to be proud of. Only the human says done.
