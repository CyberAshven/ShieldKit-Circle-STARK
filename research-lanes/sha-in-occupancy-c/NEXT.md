# Next (this ruleset)

A wall is a number, then the next construction.

Starting artifact: occupancy + HASH_BIT host at `1b18a98`, Chipnet `5de68272…` / **99144 B**, headroom **856 B** to the 100 kB relay cap. Unlocking omits rho/owner/amount8. Miner does not run SHA of the note. That is evidence. It is not B under RULES.md.

## First construction that can change §6

Put SHA residuals of leaf↔nf↔amount into occupancy composition C. Same 36 queries. Same leftover FRI engine. Spend or replace the 7200 B SHA-LDE mix slot; do not stack HASH_BIT sticker-matching. Do not add 36 extra SHA-AIR inputs (**94788 B**).

Measure: tx bytes, per-input density, leftover width, packed AIR. Mixed proveFromTLde (victim leaf + attacker nf + garbage amountCommit) must JS-fail and VM-reject **on the SHA relation**.

Do not start by packing more Merkle, shrinking A, or dropping queries/grind/TRACE to fit.

## Then, without dropping §2 or §1

RULES §1 stays gating: tx ≤ 100000, unlocking/redeem ≤ 10000, `createVirtualMachineBch2026(true)`, Electrum.

## Not a win

First new byte-count wall. Green tests on a weaker statement. Relabeling `5de68272…`, `60d186de…`, the 91 KB freeze, or the 1 MB fork as this B. `standard=false`. 36 extra SHA-AIR inputs. “The lane cannot.”
